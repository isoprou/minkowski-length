// Calculates all of the vectors between two points in set X in R^3

findsegs:=function(X)
segs:=[];
n:=#X;
for i in [1..n-1] do
    for j in [i+1..n] do
    	seg:=[(Vector(X[i])-Vector(X[j]))[k] : k in [1..3]];
    	segs:=Append(segs,seg);
    end for;
end for;
return segs;
end function;
